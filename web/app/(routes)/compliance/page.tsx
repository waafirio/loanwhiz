import { PagePlaceholder } from "@/components/page-placeholder";

export default function CompliancePage() {
  return (
    <PagePlaceholder
      title="Compliance"
      description="Covenant monitor output — active triggers and near-misses."
    />
  );
}
