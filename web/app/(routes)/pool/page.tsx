import { PagePlaceholder } from "@/components/page-placeholder";

export default function PoolPage() {
  return (
    <PagePlaceholder
      title="Pool & Performance"
      description="Per-period pool analytics across the reported ESMA tapes."
    />
  );
}
